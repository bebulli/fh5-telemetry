package com.fh5.telemetry.tuning;

/** Handling complaints the driver can report directly, on top of whatever the telemetry shows. */
public enum DrivingSymptom {
    UNDERSTEER,
    OVERSTEER,
    TRACTION_LOSS
}
